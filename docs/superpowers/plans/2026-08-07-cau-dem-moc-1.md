# Câu đệm thông minh — Mốc 1: hết lặp

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Câu đệm hết lặp — kho từ 14 câu (thực dùng chỉ 2) lên 28 câu chia ba rổ độ dài, tiếng dựng sẵn cất ra đĩa nên thêm câu không đội chi phí khởi động.

**Architecture:** Tách logic thuần (đọc kho, luật chọn, vân tay) ra hai module không đụng `torch` để test được trên máy Mac. `tts_service` chỉ còn phần dính GPU: dựng tiếng, đọc/ghi WAV. `streaming_pipeline` gỡ hai hằng số cứng, chuyển sang đọc kho.

**Tech Stack:** Python 3.14, FastAPI, pytest (thêm mới), soundfile, F5-TTS.

**Spec:** `docs/superpowers/specs/2026-08-07-cau-dem-thong-minh-design.md`

## Global Constraints

- **Không import `torch` trong `filler_store.py` và `filler_pick.py`.** `backend/core/device.py:3` import torch ở tầng module, nên bất cứ thứ gì chạm `tts_service` đều kéo theo torch. Hai module này phải test được không cần GPU.
- **Mốc 1 chỉ seed câu `chu_de: "chung"`.** Trường `chu_de` có trong lược đồ từ đầu, nhưng lọc theo chủ đề là mốc 2. Seed câu nêu chủ đề ngay bây giờ thì nó phát cho câu hỏi bất kỳ — tệ hơn hiện tại.
- **F5 không bao giờ chạy trong cuộc gọi.** Dựng tiếng chỉ xảy ra lúc khởi động (khi thiếu file) hoặc lúc thêm giọng.
- Tần số lấy mẫu F5: **24000 Hz, 16-bit mono** (`tts_service.py:413`).
- Vân tay gồm: `text + tên giọng + f5tts_nfe_step + f5tts_speed + f5tts_ref_text`.
- Ngưỡng rổ: ngắn `< 800ms`, vừa `800–1500ms`, dài `>= 1500ms`.
- Khoảng "vừa khít": `[min_ms, min_ms + 800]`.
- Giữ nguyên `_FILLER_BO_QUA_MS = 700` và `_filler_min_ms()` — không đụng.
- Chữ tiếng Việt có dấu trong `fillers.json`, đọc/ghi bằng `encoding="utf-8"` và `ensure_ascii=False`.

## Cấu trúc file

| File | Trách nhiệm | Đụng torch? |
|---|---|---|
| `backend/services/filler_store.py` (mới) | Kiểu dữ liệu `ChuDe`/`CauDem`, nạp & xác thực `fillers.json`, tính vân tay, singleton `lay_kho()` | Không |
| `backend/services/filler_pick.py` (mới) | Luật chọn thuần: vào `[(id, ms)]` + `min_ms` + số đếm, ra `id` | Không |
| `data/fillers.json` (mới) | 28 câu `chung` + chủ đề `chung` | — |
| `backend/services/tts_service.py` (sửa) | Cache đĩa theo vân tay, `pick_filler` mới, xoá `get_filler` | Có |
| `backend/pipeline/streaming_pipeline.py` (sửa) | Gỡ 2 hằng số cứng, `_send_filler` đọc kho, `dem_filler` | Có |
| `backend/pipeline/session_manager.py` (sửa) | Thay `filler_vua_dung` bằng `dem_filler` | Không |
| `startup.py`, `websocket.py`, `phone_call_service.py`, `scripts/phone_cat_loi.py`, `scripts/goi_va_noi.py` (sửa) | Năm nơi gọi `presynthesize_fillers` → `dung_fillers(lay_kho().cau)` | Có |
| `tests/` (mới) | Test đơn vị cho hai module thuần | Không |
| `scripts/do_cau_dem.py` (mới) | Đo thật: đếm số câu khác nhau trong 10 lượt | — |

---

### Task 1: Dựng hạ tầng test + kiểu dữ liệu và nạp kho

**Files:**
- Create: `requirements-dev.txt`
- Create: `pytest.ini`
- Create: `backend/services/filler_store.py`
- Test: `tests/test_filler_store.py`

**Interfaces:**
- Consumes: không
- Produces:
  - `@dataclass(frozen=True) class ChuDe: id: str; ten: str; tu_khoa: tuple[str, ...]`
  - `@dataclass(frozen=True) class CauDem: id: str; text: str; chu_de: str; hop_cau_hoi: bool`
  - `@dataclass(frozen=True) class Kho: chu_de: tuple[ChuDe, ...]; cau: tuple[CauDem, ...]`
  - `class LoiKho(ValueError)` — lỗi dữ liệu kho
  - `def nap(duong_dan: Path) -> Kho`

- [ ] **Step 1: Thêm pytest**

Tạo `requirements-dev.txt`:

```
# Chỉ dùng khi phát triển. KHÔNG cài trên máy chạy thật.
pytest>=8.0
```

Tạo `pytest.ini`:

```ini
[pytest]
pythonpath = .
testpaths = tests
```

Cài: `.venv/bin/pip install -r requirements-dev.txt`

- [ ] **Step 2: Viết test thất bại**

Tạo `tests/test_filler_store.py`:

```python
import json
from pathlib import Path

import pytest

from backend.services.filler_store import CauDem, ChuDe, LoiKho, nap


def _ghi(tmp_path: Path, du_lieu: dict) -> Path:
    p = tmp_path / "fillers.json"
    p.write_text(json.dumps(du_lieu, ensure_ascii=False), encoding="utf-8")
    return p


HOP_LE = {
    "chu_de": [{"id": "chung", "ten": "Chung", "tu_khoa": []}],
    "cau": [
        {"id": "chung_01", "text": "Dạ", "chu_de": "chung", "hop_cau_hoi": True},
        {"id": "chung_02", "text": "Vâng ạ", "chu_de": "chung", "hop_cau_hoi": False},
    ],
}


def test_nap_kho_hop_le(tmp_path):
    kho = nap(_ghi(tmp_path, HOP_LE))
    assert kho.chu_de == (ChuDe(id="chung", ten="Chung", tu_khoa=()),)
    assert kho.cau == (
        CauDem(id="chung_01", text="Dạ", chu_de="chung", hop_cau_hoi=True),
        CauDem(id="chung_02", text="Vâng ạ", chu_de="chung", hop_cau_hoi=False),
    )


def test_id_cau_trung_thi_bao_loi(tmp_path):
    xau = json.loads(json.dumps(HOP_LE))
    xau["cau"][1]["id"] = "chung_01"
    with pytest.raises(LoiKho, match="trùng"):
        nap(_ghi(tmp_path, xau))


def test_chu_de_khong_ton_tai_thi_bao_loi(tmp_path):
    xau = json.loads(json.dumps(HOP_LE))
    xau["cau"][0]["chu_de"] = "khong_co"
    with pytest.raises(LoiKho, match="không có chủ đề"):
        nap(_ghi(tmp_path, xau))


def test_text_rong_thi_bao_loi(tmp_path):
    xau = json.loads(json.dumps(HOP_LE))
    xau["cau"][0]["text"] = "   "
    with pytest.raises(LoiKho, match="rỗng"):
        nap(_ghi(tmp_path, xau))


def test_hop_cau_hoi_mac_dinh_la_true(tmp_path):
    thieu = {
        "chu_de": [{"id": "chung", "ten": "Chung", "tu_khoa": []}],
        "cau": [{"id": "c1", "text": "Dạ", "chu_de": "chung"}],
    }
    assert nap(_ghi(tmp_path, thieu)).cau[0].hop_cau_hoi is True


def test_file_khong_ton_tai_thi_bao_loi(tmp_path):
    with pytest.raises(LoiKho, match="không đọc được"):
        nap(tmp_path / "khong_co.json")
```

- [ ] **Step 3: Chạy test để chắc nó thất bại**

Run: `.venv/bin/pytest tests/test_filler_store.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'backend.services.filler_store'`

- [ ] **Step 4: Viết bản cài đặt tối thiểu**

Tạo `backend/services/filler_store.py`:

```python
"""Kho câu đệm: đọc, xác thực, và tính vân tay.

KHÔNG import torch (dù gián tiếp). Module này phải chạy được trên máy không
GPU để test - `backend.core.device` import torch ở tầng module, nên mọi thứ
chạm `tts_service` đều kéo theo nó.
"""
import json
import logging
from dataclasses import dataclass
from pathlib import Path

logger = logging.getLogger(__name__)


class LoiKho(ValueError):
    """Dữ liệu kho câu đệm sai. Nêu rõ câu nào sai để còn sửa được file."""


@dataclass(frozen=True)
class ChuDe:
    id: str
    ten: str
    tu_khoa: tuple[str, ...]


@dataclass(frozen=True)
class CauDem:
    id: str
    text: str
    chu_de: str
    hop_cau_hoi: bool


@dataclass(frozen=True)
class Kho:
    chu_de: tuple[ChuDe, ...]
    cau: tuple[CauDem, ...]


def nap(duong_dan: Path) -> Kho:
    """Đọc và xác thực `fillers.json`. Ném `LoiKho` nếu dữ liệu sai."""
    try:
        raw = json.loads(Path(duong_dan).read_text(encoding="utf-8"))
    except (OSError, json.JSONDecodeError) as e:
        raise LoiKho(f"không đọc được kho câu đệm {duong_dan}: {e}") from e

    chu_de = tuple(
        ChuDe(id=c["id"], ten=c.get("ten", c["id"]),
              tu_khoa=tuple(c.get("tu_khoa", [])))
        for c in raw.get("chu_de", [])
    )
    ten_chu_de = {c.id for c in chu_de}

    cau: list[CauDem] = []
    da_thay: set[str] = set()
    for c in raw.get("cau", []):
        cid = c["id"]
        if cid in da_thay:
            raise LoiKho(f"id câu đệm trùng: {cid!r}")
        da_thay.add(cid)
        text = (c.get("text") or "").strip()
        if not text:
            raise LoiKho(f"câu đệm {cid!r} có text rỗng")
        if c["chu_de"] not in ten_chu_de:
            raise LoiKho(f"câu đệm {cid!r} không có chủ đề {c['chu_de']!r}")
        cau.append(CauDem(id=cid, text=text, chu_de=c["chu_de"],
                          hop_cau_hoi=bool(c.get("hop_cau_hoi", True))))

    return Kho(chu_de=chu_de, cau=tuple(cau))
```

- [ ] **Step 5: Chạy test để chắc nó qua**

Run: `.venv/bin/pytest tests/test_filler_store.py -v`
Expected: PASS — 6 passed

- [ ] **Step 6: Commit**

```bash
git add requirements-dev.txt pytest.ini backend/services/filler_store.py tests/test_filler_store.py
git commit -m "feat(cau-dem): kho cau dem doc tu JSON + ha tang pytest"
```

---

### Task 2: Vân tay

**Files:**
- Modify: `backend/services/filler_store.py`
- Test: `tests/test_filler_store.py`

**Interfaces:**
- Consumes: Task 1 (`CauDem`)
- Produces: `def van_tay(text: str, giong: str, nfe: int, speed: float, ref_text: str) -> str` — chuỗi hex 12 ký tự, an toàn để đặt tên file.

- [ ] **Step 1: Viết test thất bại**

Thêm vào cuối `tests/test_filler_store.py`:

```python
from backend.services.filler_store import van_tay

_GOC = dict(text="Dạ", giong="fosd_1", nfe=16, speed=1.0, ref_text="xin chào")


def test_van_tay_on_dinh_giua_hai_lan_goi():
    assert van_tay(**_GOC) == van_tay(**_GOC)


def test_van_tay_an_toan_lam_ten_file():
    vt = van_tay(**_GOC)
    assert len(vt) == 12
    assert all(k in "0123456789abcdef" for k in vt)


@pytest.mark.parametrize("truong,gia_tri_moi", [
    ("text", "Vâng ạ"),
    ("giong", "giong_khac"),
    ("nfe", 12),
    ("speed", 1.2),
    ("ref_text", "câu mẫu khác"),
])
def test_van_tay_doi_khi_bat_ky_tham_so_nao_doi(truong, gia_tri_moi):
    khac = {**_GOC, truong: gia_tri_moi}
    assert van_tay(**khac) != van_tay(**_GOC)
```

- [ ] **Step 2: Chạy test để chắc nó thất bại**

Run: `.venv/bin/pytest tests/test_filler_store.py -k van_tay -v`
Expected: FAIL — `ImportError: cannot import name 'van_tay'`

- [ ] **Step 3: Viết bản cài đặt tối thiểu**

Thêm vào `backend/services/filler_store.py` (sau phần import, thêm `import hashlib`):

```python
def van_tay(text: str, giong: str, nfe: int, speed: float, ref_text: str) -> str:
    """Vân tay của MỘT bản tiếng câu đệm.

    Thiếu cái này là bẫy im lặng: đổi `nfe` hay `speed` trong cài đặt thì câu
    trả lời thật đọc theo tham số mới, còn câu đệm vẫn là tiếng cũ trên đĩa ->
    khách nghe hai chất giọng nối liền nhau ngay đầu mỗi lượt. Không có gì báo
    lỗi, log vẫn sạch.

    `speed` ép về chuỗi 3 số lẻ để 1.0 và 1.000 ra cùng một vân tay.
    """
    mau = f"{text}\x00{giong}\x00{nfe}\x00{speed:.3f}\x00{ref_text}"
    return hashlib.sha256(mau.encode("utf-8")).hexdigest()[:12]
```

- [ ] **Step 4: Chạy test để chắc nó qua**

Run: `.venv/bin/pytest tests/test_filler_store.py -v`
Expected: PASS — 13 passed

- [ ] **Step 5: Commit**

```bash
git add backend/services/filler_store.py tests/test_filler_store.py
git commit -m "feat(cau-dem): van tay cho ban tieng da dung"
```

---

### Task 3: Luật chọn thuần

**Files:**
- Create: `backend/services/filler_pick.py`
- Test: `tests/test_filler_pick.py`

**Interfaces:**
- Consumes: không (nhận `list[tuple[str, float]]`, không phụ thuộc kiểu của Task 1)
- Produces:
  ```python
  def chon(ung_vien: list[tuple[str, float]], min_ms: float,
           dem: dict[str, int], rng: random.Random | None = None) -> str | None
  ```
  `ung_vien` là `[(cau_id, do_dai_ms)]`. Trả `cau_id`, hoặc `None` khi rỗng.

- [ ] **Step 1: Viết test thất bại**

Tạo `tests/test_filler_pick.py`:

```python
import random

from backend.services.filler_pick import chon

RNG = lambda: random.Random(0)   # noqa: E731 - cố định để test lặp lại được


def test_rong_thi_tra_none():
    assert chon([], min_ms=900, dem={}, rng=RNG()) is None


def test_uu_tien_cau_vua_khit_thay_vi_cau_dai_nhat():
    # Cần che 900ms. Câu 2600ms tuy "đủ dài" nhưng đẩy câu trả lời thật lùi
    # 1700ms vô ích - đúng lỗi mà luật cũ mắc.
    assert chon([("a", 900.0), ("b", 2600.0)], min_ms=900, dem={}, rng=RNG()) == "a"


def test_noi_ra_ngoai_khoang_vua_khit_khi_khong_con_gi_khac():
    assert chon([("b", 2600.0)], min_ms=900, dem={}, rng=RNG()) == "b"


def test_khong_cau_nao_du_dai_thi_lay_cau_dai_nhat():
    # Khách chắc chắn nghe một quãng lặng; việc còn lại là làm nó ngắn nhất.
    assert chon([("a", 300.0), ("b", 800.0)], min_ms=1800, dem={}, rng=RNG()) == "b"


def test_duyet_het_nhom_roi_moi_lap_lai():
    ung_vien = [(f"c{i}", 1000.0) for i in range(5)]
    dem: dict[str, int] = {}
    r = RNG()
    ra = []
    for _ in range(5):
        cid = chon(ung_vien, min_ms=900, dem=dem, rng=r)
        ra.append(cid)
        dem[cid] = dem.get(cid, 0) + 1
    assert sorted(ra) == ["c0", "c1", "c2", "c3", "c4"]


def test_nhom_hai_cau_thi_luan_phien_khong_ket():
    ung_vien = [("a", 1000.0), ("b", 1000.0)]
    dem: dict[str, int] = {}
    r = RNG()
    ra = []
    for _ in range(4):
        cid = chon(ung_vien, min_ms=900, dem=dem, rng=r)
        ra.append(cid)
        dem[cid] = dem.get(cid, 0) + 1
    assert ra.count("a") == 2 and ra.count("b") == 2


def test_it_dung_nhat_duoc_uu_tien_hon_ca_do_vua_khit():
    # "a" vừa khít nhưng đã dùng 2 lần; "b" chưa dùng -> phải ra "b".
    ra = chon([("a", 900.0), ("b", 1000.0)], min_ms=900,
              dem={"a": 2}, rng=RNG())
    assert ra == "b"
```

- [ ] **Step 2: Chạy test để chắc nó thất bại**

Run: `.venv/bin/pytest tests/test_filler_pick.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'backend.services.filler_pick'`

- [ ] **Step 3: Viết bản cài đặt tối thiểu**

Tạo `backend/services/filler_pick.py`:

```python
"""Luật chọn câu đệm. Thuần logic, KHÔNG import torch - xem filler_store."""
import random

# Câu dài hơn mức cần bao nhiêu thì vẫn coi là "vừa khít". Quá số này thì nó
# đẩy câu trả lời thật lùi lại một cách vô ích.
NOI_RONG_MS = 800.0


def chon(ung_vien: list[tuple[str, float]], min_ms: float,
         dem: dict[str, int], rng: random.Random | None = None) -> str | None:
    """Chọn một câu đệm trong `ung_vien` = [(id, độ dài ms)].

    Ba tầng, rơi dần:
      1. Vừa khít: độ dài trong [min_ms, min_ms + NOI_RONG_MS]
      2. Đủ dài: độ dài >= min_ms
      3. Không câu nào đủ -> câu DÀI NHẤT (làm quãng lặng ngắn nhất có thể)

    Trong mỗi tầng, chỉ xét nhóm có SỐ ĐẾM NHỎ NHẤT rồi bốc ngẫu nhiên trong
    đó. Nhờ vậy nhóm 10 câu bảo đảm dùng hết 10 câu mới lặp lại câu đầu - chắc
    chắn, không phải xác suất như cách "tránh 3 câu vừa dùng" trước đây.
    """
    if not ung_vien:
        return None
    r = rng or random.Random()

    def it_dung_nhat(nhom: list[tuple[str, float]]) -> list[tuple[str, float]]:
        thap_nhat = min(dem.get(cid, 0) for cid, _ in nhom)
        return [(cid, ms) for cid, ms in nhom if dem.get(cid, 0) == thap_nhat]

    vua_khit = [x for x in ung_vien if min_ms <= x[1] <= min_ms + NOI_RONG_MS]
    if vua_khit:
        return r.choice(it_dung_nhat(vua_khit))[0]

    du_dai = [x for x in ung_vien if x[1] >= min_ms]
    if du_dai:
        return r.choice(it_dung_nhat(du_dai))[0]

    return max(it_dung_nhat(ung_vien), key=lambda x: x[1])[0]
```

- [ ] **Step 4: Chạy test để chắc nó qua**

Run: `.venv/bin/pytest tests/test_filler_pick.py -v`
Expected: PASS — 7 passed

- [ ] **Step 5: Commit**

```bash
git add backend/services/filler_pick.py tests/test_filler_pick.py
git commit -m "feat(cau-dem): luat chon vua khit + chong lap bang so dem"
```

---

### Task 4: Kho câu thật + singleton

**Files:**
- Create: `data/fillers.json`
- Modify: `backend/services/filler_store.py`
- Test: `tests/test_kho_that.py`

**Interfaces:**
- Consumes: Task 1 (`nap`, `Kho`)
- Produces:
  - `DUONG_DAN_MAC_DINH: Path` — `Path("data/fillers.json")`
  - `def lay_kho() -> Kho` — nạp một lần rồi nhớ; `def nap_lai() -> Kho` — xoá bộ nhớ và nạp lại.

- [ ] **Step 1: Viết test thất bại**

Tạo `tests/test_kho_that.py`:

```python
"""Kiểm kho câu thật trong data/fillers.json - bắt lỗi lúc soạn câu."""
from backend.services.filler_store import DUONG_DAN_MAC_DINH, lay_kho, nap


def test_kho_that_nap_duoc():
    kho = nap(DUONG_DAN_MAC_DINH)
    assert len(kho.cau) >= 28


def test_moc_1_chi_co_chu_de_chung():
    # Lọc theo chủ đề là mốc 2. Seed câu nêu chủ đề bây giờ thì nó phát cho
    # câu hỏi bất kỳ - tệ hơn hiện tại.
    assert {c.chu_de for c in nap(DUONG_DAN_MAC_DINH).cau} == {"chung"}


def test_du_cau_ngan_va_cau_dai():
    # Xấp xỉ theo số ký tự, KHÔNG phải bảo đảm về mili giây: độ dài thật đo
    # lúc dựng tiếng và khác nhau theo giọng. Đây chỉ để bắt lỗi soạn thiếu
    # nguyên một rổ. Số ms thật kiểm bằng scripts/do_cau_dem.py.
    cau = nap(DUONG_DAN_MAC_DINH).cau
    assert sum(1 for c in cau if len(c.text) <= 20) >= 8
    assert sum(1 for c in cau if len(c.text) >= 55) >= 10


def test_lay_kho_nho_ket_qua():
    assert lay_kho() is lay_kho()
```

- [ ] **Step 2: Chạy test để chắc nó thất bại**

Run: `.venv/bin/pytest tests/test_kho_that.py -v`
Expected: FAIL — `ImportError: cannot import name 'DUONG_DAN_MAC_DINH'`

- [ ] **Step 3: Tạo kho câu**

Tạo `data/fillers.json`. 28 câu, cố ý **không** câu nào cũng khuôn "Dạ … ạ" — câu đệm phát ở đầu mỗi lượt, bốn câu cùng khuôn là khách nghe một điệp khúc suốt cuộc gọi:

```json
{
  "chu_de": [
    { "id": "chung", "ten": "Dùng được mọi lúc", "tu_khoa": [] }
  ],
  "cau": [
    { "id": "ngan_01", "text": "Dạ", "chu_de": "chung", "hop_cau_hoi": true },
    { "id": "ngan_02", "text": "Vâng ạ", "chu_de": "chung", "hop_cau_hoi": true },
    { "id": "ngan_03", "text": "Dạ vâng ạ", "chu_de": "chung", "hop_cau_hoi": true },
    { "id": "ngan_04", "text": "Được rồi ạ", "chu_de": "chung", "hop_cau_hoi": true },
    { "id": "ngan_05", "text": "Dạ em nghe", "chu_de": "chung", "hop_cau_hoi": true },
    { "id": "ngan_06", "text": "Vâng em nghe", "chu_de": "chung", "hop_cau_hoi": true },
    { "id": "ngan_07", "text": "Dạ em rõ", "chu_de": "chung", "hop_cau_hoi": false },
    { "id": "ngan_08", "text": "Vâng, em nghe đây", "chu_de": "chung", "hop_cau_hoi": true },

    { "id": "vua_01", "text": "Vâng, để em xem", "chu_de": "chung", "hop_cau_hoi": true },
    { "id": "vua_02", "text": "Dạ, em kiểm tra ngay", "chu_de": "chung", "hop_cau_hoi": true },
    { "id": "vua_03", "text": "Vâng, anh chị chờ em chút", "chu_de": "chung", "hop_cau_hoi": true },
    { "id": "vua_04", "text": "Dạ, em xem lại chút nhé", "chu_de": "chung", "hop_cau_hoi": true },
    { "id": "vua_05", "text": "Vâng, em tra giúp anh chị nhé", "chu_de": "chung", "hop_cau_hoi": true },
    { "id": "vua_06", "text": "Dạ, cái này để em xem", "chu_de": "chung", "hop_cau_hoi": true },
    { "id": "vua_07", "text": "Vâng, em nắm được rồi", "chu_de": "chung", "hop_cau_hoi": false },
    { "id": "vua_08", "text": "Dạ, anh chị đợi em một chút", "chu_de": "chung", "hop_cau_hoi": true },
    { "id": "vua_09", "text": "Vâng, em xem ngay đây ạ", "chu_de": "chung", "hop_cau_hoi": true },
    { "id": "vua_10", "text": "Dạ, để em tra lại chút", "chu_de": "chung", "hop_cau_hoi": true },

    { "id": "dai_01", "text": "Dạ vâng, anh chị chờ em một chút để em kiểm tra lại thông tin nhé", "chu_de": "chung", "hop_cau_hoi": true },
    { "id": "dai_02", "text": "Vâng ạ, em đang tra lại thông tin cho anh chị, anh chị chờ em một lát nhé", "chu_de": "chung", "hop_cau_hoi": true },
    { "id": "dai_03", "text": "Dạ, để em xem lại cho chính xác rồi em trả lời anh chị ngay đây ạ", "chu_de": "chung", "hop_cau_hoi": true },
    { "id": "dai_04", "text": "Vâng, cái này em cần tra lại một chút, anh chị chờ em xíu nhé", "chu_de": "chung", "hop_cau_hoi": true },
    { "id": "dai_05", "text": "Dạ vâng, em xem lại giúp anh chị rồi em báo lại ngay bây giờ ạ", "chu_de": "chung", "hop_cau_hoi": true },
    { "id": "dai_06", "text": "Vâng ạ, anh chị giữ máy giúp em một lát, em tra lại rồi báo anh chị ngay", "chu_de": "chung", "hop_cau_hoi": true },
    { "id": "dai_07", "text": "Dạ, phần này em phải xem lại cho kỹ, anh chị chờ em một chút nhé", "chu_de": "chung", "hop_cau_hoi": true },
    { "id": "dai_08", "text": "Vâng, em kiểm tra lại thông tin rồi trả lời anh chị cho chính xác nhé", "chu_de": "chung", "hop_cau_hoi": true },
    { "id": "dai_09", "text": "Dạ vâng, anh chị đợi em một lát, em tra xong là em báo lại ngay ạ", "chu_de": "chung", "hop_cau_hoi": true },
    { "id": "dai_10", "text": "Vâng ạ, để em xem lại một chút rồi em nói rõ hơn cho anh chị nghe nhé", "chu_de": "chung", "hop_cau_hoi": true }
  ]
}
```

- [ ] **Step 4: Thêm singleton**

Thêm vào cuối `backend/services/filler_store.py`:

```python
DUONG_DAN_MAC_DINH = Path("data/fillers.json")

_kho: Kho | None = None


def lay_kho() -> Kho:
    """Kho câu đệm dùng chung, nạp một lần.

    Singleton như `settings`: nạp file ở mỗi lượt gọi là đọc đĩa trên đường
    găng của cuộc gọi, mà nội dung thì chỉ đổi khi người dùng sửa file.
    """
    global _kho
    if _kho is None:
        _kho = nap(DUONG_DAN_MAC_DINH)
        logger.info("Đã nạp kho câu đệm: %d câu, %d chủ đề",
                    len(_kho.cau), len(_kho.chu_de))
    return _kho


def nap_lai() -> Kho:
    """Quên bản đang nhớ và đọc lại từ đĩa (dùng sau khi sửa file)."""
    global _kho
    _kho = None
    return lay_kho()
```

- [ ] **Step 5: Chạy test để chắc nó qua**

Run: `.venv/bin/pytest tests/ -v`
Expected: PASS — 17 passed

- [ ] **Step 6: Commit**

```bash
git add data/fillers.json backend/services/filler_store.py tests/test_kho_that.py
git commit -m "feat(cau-dem): 28 cau chung chia ba ro do dai + singleton kho"
```

---

### Task 5: Cache tiếng ra đĩa trong tts_service

**Files:**
- Modify: `backend/services/tts_service.py:60-61` (khoá cache), `:355-361` (`drop_voice`), `:505-530` (`presynthesize_fillers`), `:583-596` (xoá `get_filler`)
- Test: `scripts/do_cau_dem.py` (Task 8) — phần này chạm GPU nên không test đơn vị được

**Interfaces:**
- Consumes: Task 2 (`van_tay`), Task 4 (`lay_kho`)
- Produces:
  - `THU_MUC_FILLER: Path` = `Path("data/fillers_wav")`
  - `async def dung_fillers(self, cau: list[CauDem], voice: str | None = None) -> None` — thay `presynthesize_fillers`
  - Khoá cache đổi từ `(voice, text)` sang `(voice, cau_id)`

- [ ] **Step 1: Đổi khoá cache và thêm đường dẫn**

Trong `backend/services/tts_service.py`, sửa `__init__` (dòng 60-61) — chỉ đổi chú thích, kiểu giữ nguyên:

```python
        self._filler_cache: dict[tuple[str, str], bytes] = {}   # (voice, cau_id) -> wav
        self._filler_ms: dict[tuple[str, str], float] = {}      # (voice, cau_id) -> ms
```

Thêm vào khối import đầu file (`filler_store` và `filler_pick` không import gì từ
`backend`, nên **không có nguy cơ vòng lặp import** — đặt ở tầng module, đừng import
trong thân hàm):

```python
from backend.services.filler_store import CauDem, van_tay
from backend.services.filler_pick import chon as _chon_filler
```

Thêm ngay dưới phần `SILENCE_PEAK` ở đầu file:

```python
# Tiếng câu đệm cất ở đây để khởi động không phải gọi F5 lại. Tên file mang vân
# tay: đổi nfe/speed/giọng là vân tay lệch -> dựng lại đúng câu đó.
THU_MUC_FILLER = Path("data/fillers_wav")
```

- [ ] **Step 2: Thay `presynthesize_fillers` bằng `dung_fillers`**

Thay toàn bộ thân `presynthesize_fillers` (dòng 505-530) bằng:

```python
    def _duong_dan_filler(self, voice: str, cau_id: str, vt: str) -> Path:
        return THU_MUC_FILLER / voice / f"{cau_id}__{vt}.wav"

    def _van_tay_filler(self, text: str, voice: str) -> str:
        return van_tay(text, voice, settings.f5tts_nfe_step,
                       settings.f5tts_speed, settings.f5tts_ref_text)

    async def dung_fillers(self, cau: list[CauDem], voice: str | None = None):
        """Bảo đảm mọi câu đệm đều có tiếng sẵn sàng cho giọng này.

        Đọc từ đĩa trước, chỉ gọi F5 cho những câu còn thiếu hoặc lệch vân tay.
        Nhờ vậy khởi động lần hai trở đi KHÔNG chạm GPU - quan trọng vì
        `_warm_fillers` chạy đúng lúc cuộc gọi bắt đầu (api/websocket.py:286),
        và 28 câu gọi F5 nền sẽ giành GPU với chính cuộc gọi đang sống.
        """
        voice = await self.ensure_voice(voice or self._default_voice)
        thu_muc = THU_MUC_FILLER / voice
        thu_muc.mkdir(parents=True, exist_ok=True)

        doc_dia = dung_moi = 0
        for c in cau:
            khoa = (voice, c.id)
            if khoa in self._filler_cache:
                continue
            vt = self._van_tay_filler(c.text, voice)
            p = self._duong_dan_filler(voice, c.id, vt)
            if p.exists():
                try:
                    wav = p.read_bytes()
                    self._filler_cache[khoa] = wav
                    self._filler_ms[khoa] = self._wav_duration_ms(wav)
                    doc_dia += 1
                    continue
                except OSError as e:
                    logger.warning("Đọc %s hỏng, dựng lại: %s", p, e)
            try:
                wav = await self.synthesize(c.text, voice=voice)
            except Exception as e:
                logger.warning("Không dựng được câu đệm %r: %s", c.id, e)
                continue
            # Xoá bản vân tay cũ của cùng câu, nếu không đĩa phình mãi mỗi lần
            # đổi nfe.
            for cu in thu_muc.glob(f"{c.id}__*.wav"):
                cu.unlink(missing_ok=True)
            p.write_bytes(wav)
            self._filler_cache[khoa] = wav
            self._filler_ms[khoa] = self._wav_duration_ms(wav)
            dung_moi += 1

        logger.info("Câu đệm [%s]: %d đọc từ đĩa, %d dựng mới, tổng %d",
                    voice, doc_dia, dung_moi,
                    sum(1 for k in self._filler_cache if k[0] == voice))
```

- [ ] **Step 3: Xoá `get_filler`**

Xoá toàn bộ `get_filler` (dòng 583-596). Không nơi nào gọi nó — chỉ còn một dòng chú thích ở `api/websocket.py:20`, sửa dòng đó thành "Thiếu chúng thì `pick_filler` trả None".

- [ ] **Step 4: Kiểm tra import không hỏng**

Run: `.venv/bin/python -c "import backend.services.tts_service"`
Expected: không lỗi (torch nạp chậm, chờ vài giây là bình thường)

- [ ] **Step 5: Commit**

```bash
git add backend/services/tts_service.py backend/api/websocket.py
git commit -m "feat(cau-dem): cache tieng ra dia theo van tay, bo get_filler"
```

---

### Task 6: `pick_filler` mới

**Files:**
- Modify: `backend/services/tts_service.py:542-581`
- Test: `tests/test_filler_pick.py` đã phủ luật; phần này chỉ là lớp mỏng

**Interfaces:**
- Consumes: Task 3 (`filler_pick.chon`), Task 4 (`CauDem`)
- Produces:
  ```python
  def _giong_thuc(self, voice: str | None) -> str
  def do_dai_filler(self, cau_id: str, voice: str | None = None) -> float
  def pick_filler(self, cau: list[CauDem], voice: str | None = None,
                  min_ms: float = 0.0,
                  dem: dict[str, int] | None = None
                  ) -> tuple[bytes | None, CauDem | None]
  ```
  `pick_filler` trả `(wav_bytes, CauDem)` hoặc `(None, None)`.

- [ ] **Step 1: Thay thân `pick_filler`**

Thay toàn bộ `pick_filler` (dòng 542-581) bằng:

```python
    def _giong_thuc(self, voice: str | None) -> str:
        """Quy tên giọng lạ về giọng mặc định.

        Phiên mới mặc định voice_name="default" trong khi giọng thật tên theo
        file (vd "giong_ngan") - không quy về thì mọi tra cứu cache đều trượt và
        câu đệm chưa bao giờ phát.
        """
        name = voice or self._default_voice
        return name if name in self._voices else self._default_voice

    def do_dai_filler(self, cau_id: str, voice: str | None = None) -> float:
        """Độ dài tiếng (ms) của một câu đệm, 0 nếu chưa dựng."""
        return self._filler_ms.get((self._giong_thuc(voice), cau_id), 0.0)

    def pick_filler(self, cau: list[CauDem], voice: str | None = None,
                    min_ms: float = 0.0,
                    dem: dict[str, int] | None = None
                    ) -> tuple[bytes | None, CauDem | None]:
        """Chọn câu đệm đã có tiếng cho giọng này. Trả (wav, CauDem).

        Lớp mỏng: luật chọn nằm ở services/filler_pick.py để test được mà không
        cần GPU. Ở đây chỉ lo tra cache và quy giọng lạ về giọng mặc định.
        """
        name = self._giong_thuc(voice)
        theo_id = {c.id: c for c in cau}
        ung_vien = [(c.id, self._filler_ms[(name, c.id)])
                    for c in cau if (name, c.id) in self._filler_cache]
        chon_id = _chon_filler(ung_vien, min_ms=min_ms, dem=dem or {})
        if chon_id is None:
            return None, None
        return self._filler_cache[(name, chon_id)], theo_id[chon_id]
```

Dùng `_giong_thuc` luôn cho `co_filler` (dòng 498-503) và `dung_fillers` để ba chỗ quy
giọng giống hệt nhau — hiện `co_filler` và `pick_filler` đang lặp cùng ba dòng đó.

- [ ] **Step 2: Kiểm tra import không hỏng**

Run: `.venv/bin/python -c "import backend.services.tts_service"`
Expected: không lỗi

- [ ] **Step 3: Commit**

```bash
git add backend/services/tts_service.py
git commit -m "refactor(cau-dem): pick_filler uy quyen cho filler_pick"
```

---

### Task 7: Nối vào pipeline

**Files:**
- Modify: `backend/pipeline/streaming_pipeline.py:28-75` (gỡ 2 hằng số), `:517-539` (`_send_filler`)
- Modify: `backend/pipeline/session_manager.py` — khai báo `dem_filler` trong `__init__` (cạnh `self.spec_stt`, dòng 106). **KHÔNG đụng `clear_speculation`** — xem Step 3.
- Modify: `backend/core/startup.py:12,82`
- Modify: `backend/api/websocket.py:8,17-24`
- Modify: `backend/services/phone_call_service.py:1386`
- Modify: `scripts/phone_cat_loi.py:131`
- Modify: `scripts/goi_va_noi.py:169`

**Interfaces:**
- Consumes: Task 4 (`lay_kho`), Task 6 (`pick_filler`)
- Produces: `session.dem_filler: dict[str, int]` thay cho `session.filler_vua_dung`

- [ ] **Step 1: Gỡ hai hằng số cứng**

Trong `backend/pipeline/streaming_pipeline.py`, xoá `FILLER_PHRASES` (dòng 31-51) và `FILLER_HOP_CAU_HOI` (dòng 60-75) cùng khối chú thích 28-30, 52-59. Nội dung của chúng đã chuyển sang `data/fillers.json`.

Thêm vào khối import đầu file (tầng module, không import trong thân hàm):

```python
from backend.services.filler_store import lay_kho
```

- [ ] **Step 2: Thay phần chọn câu trong `_send_filler`**

Thay đoạn từ dòng 517 (`# Khách vừa HỎI…`) tới dòng 539 bằng:

```python
        # Khách vừa HỎI thì "em nắm được rồi" nghe như gạt đi - lọc bỏ những
        # câu gắn cờ không hợp câu hỏi. Chưa có phiên âm ở mốc này nên vẫn suy
        # từ lượt trước: đang tư vấn thì đa số lượt là câu hỏi. (Mốc 2 sẽ đọc
        # session.spec_stt để biết chắc thay vì đoán.)
        kho = lay_kho().cau
        if session.turn_count > 0:
            kho = [c for c in kho if c.hop_cau_hoi]

        dem = getattr(session, "dem_filler", None)
        if dem is None:
            dem = session.dem_filler = {}

        filler_audio, cau = self.tts.pick_filler(
            kho, session.voice_name, min_ms=can_che, dem=dem,
        )
        if not filler_audio:
            return
        dem[cau.id] = dem.get(cau.id, 0) + 1
        await self._send_audio(ws, filler_audio, is_filler=True, turn_id=session.turn_id)
        metrics["filler_ms"] = round((time.perf_counter() - t_start) * 1000)
        metrics["filler_text"] = cau.text
        metrics["filler_id"] = cau.id
```

- [ ] **Step 3: Đổi trạng thái phiên**

`filler_vua_dung` **không có trong `session_manager.py`** — nó chỉ được tạo động bằng
`getattr` ở `streaming_pipeline.py:526`, nên không có gì phải xoá ở đây. Chỉ thêm
khai báo mới vào `__init__` của `CallSession`, ngay dưới `self.spec_stt` (dòng 106):

```python
        # Đếm số lần mỗi câu đệm đã dùng TRONG CUỘC GỌI NÀY, để chọn câu ít
        # dùng nhất. Thuộc về phiên: khách mới thì không việc gì phải tránh
        # câu đã dùng với khách trước.
        self.dem_filler: dict[str, int] = {}
```

**TUYỆT ĐỐI KHÔNG thêm `dem_filler` vào `clear_speculation`** (dòng 169-177). Hàm đó
chạy **mỗi lượt**, không phải mỗi cuộc gọi — reset ở đó là mọi lượt đều bắt đầu với
số đếm rỗng, và cơ chế chống lặp trở lại y hệt bốc ngẫu nhiên. Đây đúng là thứ tính
năng này sinh ra để chữa, nên hỏng ở đây thì test đơn vị vẫn xanh mà cuộc gọi thật
vẫn lặp.

- [ ] **Step 4: Sửa NĂM nơi gọi `presynthesize_fillers`**

Kiểm bằng `grep -rn "presynthesize_fillers" backend/ scripts/` — có **năm** lời gọi thật,
không phải hai. Sót một chỗ là app sập lúc khởi động hoặc lúc gọi điện:

| File | Dòng |
|---|---|
| `backend/core/startup.py` | 82 |
| `backend/api/websocket.py` | 24 |
| `backend/services/phone_call_service.py` | 1386 |
| `scripts/phone_cat_loi.py` | 131 |
| `scripts/goi_va_noi.py` | 169 |

Cả năm chỗ đổi theo cùng một khuôn: `presynthesize_fillers(FILLER_PHRASES, ...)` →
`dung_fillers(lay_kho().cau, ...)`, giữ nguyên tham số `voice=` nếu chỗ đó đang có.
Mỗi file cũng phải đổi dòng import `FILLER_PHRASES` sang `lay_kho`.

Hai script trong `scripts/` import `FILLER_PHRASES` từ `backend.pipeline.streaming_pipeline`
— hằng số đó đã bị xoá ở Step 1, nên không sửa là chúng gãy ngay khi chạy.

`backend/core/startup.py` — đổi dòng 12 và 82:

```python
from backend.pipeline.streaming_pipeline import StreamingPipeline
from backend.services.filler_store import lay_kho
```

```python
        await state.tts.dung_fillers(lay_kho().cau)
```

`backend/api/websocket.py` — đổi dòng 8 và thân `_warm_fillers`:

```python
from backend.services.filler_store import lay_kho
```

```python
            await tts.dung_fillers(lay_kho().cau, voice=voice)
```

- [ ] **Step 5: Kiểm tra không còn tham chiếu cũ**

Run:
```bash
grep -rn "FILLER_PHRASES\|FILLER_HOP_CAU_HOI\|presynthesize_fillers\|filler_vua_dung\|get_filler" backend/ scripts/
```
Expected: chỉ còn trong chú thích lịch sử, không còn lời gọi thật nào.

Run: `.venv/bin/python -c "import backend.core.startup"`
Expected: không lỗi

- [ ] **Step 6: Commit**

```bash
git add backend/pipeline/streaming_pipeline.py backend/pipeline/session_manager.py backend/core/startup.py backend/api/websocket.py
git commit -m "feat(cau-dem): pipeline doc kho thay hang so cung, dem thay tranh-3-cau"
```

---

### Task 8: Đo thật trên máy Windows

**Files:**
- Create: `scripts/do_cau_dem.py`

**Interfaces:**
- Consumes: backend đang chạy tại `http://localhost:8100`
- Produces: bảng độ dài thật từng câu + số câu khác nhau trong 10 lượt

- [ ] **Step 1: Viết script đo**

Tạo `scripts/do_cau_dem.py`:

```python
"""Đo kho câu đệm trên máy có GPU. Chạy khi backend đã lên.

Hai thứ test đơn vị KHÔNG kiểm được:
  1. Độ dài THẬT mỗi câu (ms) - phụ thuộc giọng, chỉ biết sau khi F5 dựng
  2. Số câu khác nhau khách thực sự nghe trong một cuộc 10 lượt
"""
import json
import random
import sys
from collections import Counter
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
sys.stdout.reconfigure(encoding="utf-8")

from backend.services.filler_pick import chon
from backend.services.filler_store import lay_kho
from backend.services.tts_service import THU_MUC_FILLER, F5TTSService


def main():
    kho = lay_kho()
    tts = F5TTSService()
    tts.load()
    import asyncio
    giong = tts.default_voice_name()
    asyncio.run(tts.dung_fillers(list(kho.cau)))

    print(f"=== do dai that, giong '{giong}' ===")
    ro = Counter()
    for c in kho.cau:
        ms = tts.do_dai_filler(c.id, giong)
        ten_ro = "ngan" if ms < 800 else ("vua" if ms < 1500 else "dai")
        ro[ten_ro] += 1
        print(f"  {c.id:<10} {ms:6.0f} ms  {ten_ro:<5} {c.text[:52]}")
    print(f"\n  ro: ngan={ro['ngan']} vua={ro['vua']} dai={ro['dai']}")

    print("\n=== mo phong 10 luot duong thoai (min_ms=1800) ===")
    ung_vien = [(c.id, tts.do_dai_filler(c.id, giong))
                for c in kho.cau if c.hop_cau_hoi]
    dem, ra = {}, []
    r = random.Random(0)
    for _ in range(10):
        cid = chon(ung_vien, min_ms=1800, dem=dem, rng=r)
        dem[cid] = dem.get(cid, 0) + 1
        ra.append(cid)
    print("  " + " -> ".join(ra))
    print(f"\n  so cau KHAC NHAU: {len(set(ra))}/10   (truoc day: 2)")
    print(f"  DAT" if len(set(ra)) >= 8 else "  CHUA DAT (can >= 8)")

    n_file = sum(1 for _ in (THU_MUC_FILLER / giong).glob("*.wav"))
    print(f"\n  file wav tren dia: {n_file}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Đẩy code sang máy Windows và chạy**

Dùng skill `test-web-win` để đồng bộ và khởi động lại dịch vụ, rồi:

```bash
ssh win "cd C:\\duan\\chat-ai && .venv\\python.exe scripts\\do_cau_dem.py"
```

Expected: bảng độ dài, `ro: ngan>=8 vua>=8 dai>=8`, và `so cau KHAC NHAU: >=8/10  DAT`.

Nếu một rổ lệch nhiều (ví dụ `dai=3`) thì sửa câu trong `data/fillers.json` cho dài/ngắn lại rồi chạy lại — đây chính là lý do có script này.

- [ ] **Step 3: Chạy lại lần hai để chứng minh cache đĩa**

```bash
ssh win "cd C:\\duan\\chat-ai && .venv\\python.exe scripts\\do_cau_dem.py"
```

Expected: log ghi `28 đọc từ đĩa, 0 dựng mới`. Nếu vẫn dựng mới thì vân tay đang lệch — sai ở `_van_tay_filler`.

- [ ] **Step 4: Commit**

```bash
git add scripts/do_cau_dem.py
git commit -m "test(cau-dem): script do do dai that va dem cau khac nhau"
```

---

### Task 9: Gọi thử thật

**Files:** không sửa file nào — đây là bước xác nhận cuối.

- [ ] **Step 1: Chạy app và nghe một cuộc**

Dùng skill `test-app-flow` để mở app và chạy một cuộc hội thoại ít nhất 8 lượt.

- [ ] **Step 2: Đếm câu đệm trong log**

```bash
ssh win "cd C:\\duan\\chat-ai && findstr /C:\"filler\" logs\\backend.log | more"
```

Expected: `filler_id` trong 8 lượt phải có **ít nhất 6 giá trị khác nhau**. Trước đây chỉ có 2.

- [ ] **Step 3: Xác nhận không có TTS nào chạy lúc gọi**

Trong log, giữa lúc cuộc gọi bắt đầu và lượt đầu tiên **không được** có dòng `Pre-synthesizing` hay `dựng mới`.

- [ ] **Step 4: Commit ghi chú kết quả**

Cập nhật spec với số đo thật, rồi:

```bash
git add docs/superpowers/specs/2026-08-07-cau-dem-thong-minh-design.md
git commit -m "docs(cau-dem): ghi so do that sau moc 1"
```

---

## Tự soát kế hoạch

**Phủ spec (phần thuộc mốc 1):**

| Yêu cầu spec | Task |
|---|---|
| `filler_store` + `fillers.json` | 1, 4 |
| Không khai báo độ dài, đo theo giọng | 5 (`_wav_duration_ms` lúc dựng), 8 (in ra) |
| Cache đĩa | 5 |
| Vân tay | 2, 5 |
| Luật "vừa khít" | 3 |
| Chống lặp bằng số đếm, reset mỗi phiên | 3, 7 |
| Giữ `_FILLER_BO_QUA_MS`, `_filler_min_ms` | 7 (không đụng) |
| Thứ tự dựng ưu tiên `chung` | không cần ở mốc 1 — toàn bộ kho là `chung` |
| Kiểm chứng: chống lặp 5/5 | 3 |
| Kiểm chứng: vân tay đổi nfe | 2 |
| Kiểm chứng: cache đĩa không gọi F5 | 8 bước 3 |
| Kiểm chứng: vừa khít, min_ms=900 không chọn 2600ms | 3 |
| Kiểm chứng: 10 lượt ≥ 8 câu khác nhau | 8, 9 |

**Ngoài mốc 1 (không làm ở kế hoạch này):** lưới từ khoá chủ đề, nhận diện câu hỏi từ `spec_stt`, API + UI, bảng sức khoẻ, ba nút LLM offline, cột `filler_id` trong DB. Thuộc mốc 2-4.

**Nhất quán kiểu:** `pick_filler` trả `(bytes | None, CauDem | None)` ở Task 6, và Task 7 dùng đúng `cau.id` / `cau.text`. `chon()` nhận `list[tuple[str, float]]` ở Task 3 và Task 6 truyền đúng dạng đó. `van_tay()` năm tham số ở Task 2, Task 5 gọi đủ năm.
